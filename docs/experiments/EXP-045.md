# EXP-045 — NFC-C FD + LED pair vs TWN4 field

## Hypothesis

P1_1 is NTAG FD (active low). With debugger isolated, P2_1+P2_2 blink the proven LED pair (RGB then white) while TWN4 HF field is present. Without field, LED stays off.

## Firmware

`v0.7a_nfc_fd_led`. No I2C. FD input only. LED only P2_1/P2_2. Runtime GPIO27+21 OFF.

Host: `ovh-nfc field-watch` on `/dev/ttyACM0`.

## UART baseline (no TWN4)

Recapture **PASS**:

```text
EXP-045 v0.7a NFC-C FD LED
RESET_CAUSE=01
FD=01 FIELD=00
DONE
FD=01 FIELD=00 LED=00
```

Idle FD=1 **OVĚŘENO**. LED off without field **OVĚŘENO** (UART).

## TWN4 field-watch

Reader talks. `in_field: false`, hits=0 (12 s and 55 s). MCU FD stayed 1, LED=00. No RF coupling at the current geometry.

## Result

**PASS** firmware/UART/host. **FAIL** as proximity demo until TWN4 antenna is on the tag (human move). Visual blink not observed.

## Next

Person: USB TWN4 na anténu štítku, tag ON, debugger OFF:

```text
/home/hw/bin/ov26-relays.sh idle
/home/hw/bin/ov26-relays.sh tag-on
python3 /home/hw/OpenVusion26_FW/tools/nfc_gateway/cli.py --port /dev/ttyACM0 field-watch --wait 60
```

LED pár (RGB+bílá) má blikat při `in_field: true`. Samostatnou bílou GPIO neumíme.
