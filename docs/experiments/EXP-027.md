# EXP-027 — Phase F minimal COG init

## Hypothesis

Pervasive `register_data_sm` (00 0E, E5 19, E0 02, 00 CF 8D) s `_sendIndexData` framing po H-L-H je bezpečné. MCU/UART zůstanou, BUSY HIGH stačí (oficiální wait until HIGH).

## Firmware

`v0.4f_min_init` (`--nooverlay`, flattened like v0.4e). First helper-heavy image: UART garbage. Flattened reflash: PASS.

## UART

```text
RESET_CAUSE=01
W1 BUSY=1
SR=01 BUSY=1
E5=01
E0=01
PSR=01 BUSY=1
CHK P0_2SEL=0 CS=1
DONE
HB 00..16 BUSY=1
```

`captures/ov26_exp027_por.bin`

## Result

**PASS** — všechny TX, MCU stabilní, BUSY HIGH. Mapování MOSI/SCLK/DC stále REFERENCE.

## Next

EXP-028: stream 5624+5624, žádný 0x12.
