# EXP-054 — Three SHOW slots + Python menu app

## Change

1 OpenVusionHack (unchanged). 2 BWR 16-cell test (replaces TagHack). 3 Shut up and take my money.

Firmware `v0.10e_nfc_show3`. Host `tools/nfc_gateway/show_app.py`.

## Memory

RLE 26196 B. SDCC ROM after build in Result.

## Result

SDCC ROM **29364 / 32768** (3404 B free). Flashed DEV 28 KB verify OK. UART recap **PASS** `EXP-054 v0.10e NFC SHOW3` `ARMED`. TAG ON.

Menu: `/home/hw/bin/ov26-nfc-show.sh`
