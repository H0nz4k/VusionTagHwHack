# EXP-037 — RF-D bounded TX ping

## Hypothesis

Three STX bursts at PA=0x44 (−30 dBm) complete without reset storm. UART remains sane.

## Firmware

`v0.5d_rf_txping` prepared (length-byte STX then SIDLE; full PDU not required for this layer). Flattened `main.c`. `--nooverlay`.

## UART (`captures/rf/ov26_exp037_por.bin`)

350 B high-bit garbage. `EXP-037` count 0. `DONE` count 0.

## Result

**FAIL UART / INCONCLUSIVE TX.** Did not retry (avoid 3× same signature). Software STX path is compiled; **OTA = NOT VERIFIED**. No independent receiver on the bench.

## Classification

TX SOFTWARE FLOW: not UART-proven. OVER-THE-AIR: not verified.

## Stop

CC2500 module not present. Next physical step: 3.3 V CC2500 SPI on Pi, then `ovh-rf probe` (not `--dry-run`).
