# EXP-069 — NFC send known artwork instead of ramp

## HYPOTÉZA

Stejný OVMB kanál jako EXP-068 přenese skutečný 11 248 B artwork (ne `make_test_image` rampu). MCU `v0.12b` bez reflashe. Sklo ukáže čitelný obsah.

## VSTUPNÍ STAV

`v0.12b` na DEV. Config mirror beze změny. BIN z `scripts/pack_known_art_bins.py`.

## JEDNA HLAVNÍ ZMĚNA

Host `cli.py send` známých BIN místo testracké rampy.

| BIN | Zdroj | CRC32 |
|---|---|---|
| `ovhack.bin` | EXP-033 OpenVusionHack | `D9331F86` |
| `money.bin` | EXP-056 slot 3 | `ADCEA5C4` |

## SKUTEČNÝ VÝSLEDEK

Bez reflashe MCU:

| Pořadí | BIN | rc | Host |
|---|---|---|---|
| 1 | ovhack | 0 | DONE `D9331F86` 235 chunks 354.59 s |
| 2 | money | 0 | DONE `ADCEA5C4` 235 chunks 357.36 s, 1 retry |
| 3 | ovhack znovu | 0 | DONE `D9331F86` 354.37 s — poslední obraz na skle |

UART během dlouhého RF byl bez použitelných tokenů (stejný garbage jako dřív). Host CRC a DONE stačí; vizuál = lidská fotka.

Trap `idle`.

## KLASIFIKACE

**PASS** host. Vizuál čeká na fotografii OpenVusionHack (černě OpenVusion, červeně Hack).

## DALŠÍ KROK

Fotka skla. Volitelně další TagStudio export přímo z `*.tagstudio.json`.
