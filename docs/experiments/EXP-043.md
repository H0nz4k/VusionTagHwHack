# EXP-043 — NFC-B I2C session 0xFE (repeated start)

## Hypothesis

After ACK of 0xAA, I2C read of session block 0xFE (8 bytes) via repeated start + 0xAB returns session registers (read-only).

## Firmware

`v0.6d_nfc_sess`. No P1_0. No EEPROM write.

## UART (`captures/nfc/ov26_exp043_por.bin`)

Isolated **PASS**:

```text
ACKAA=01 ACKFE=01 ACKAB=00
SESS FF FF FF FF FF FF FF FF
DONE
```

Write address 0xAA ACK, word 0xFE ACK, read address 0xAB **NACK**. SDA floated as 0xFF.

## Result

**FAIL** for session payload. Write path to 0xFE **OVĚŘENO**. I2C read-address 0xAB not ACKed with this Sr sequence.

## Next

One-variable retry: STOP then START before 0xAB (EXP-044).
