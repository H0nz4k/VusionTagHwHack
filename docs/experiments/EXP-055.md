# EXP-055 — SHOW slot 4 blank white refresh

## HYPOTÉZA

Čtvrtá NFC volba může smazat sklo bez čtvrté RLE bitmapy: stejný CoG init jako 1–3, do příkazů `0x10` a `0x13` poslat 5624× `0x00` (bílá = p10=0, p13=0).

## VSTUPNÍ STAV

DEV tag, firmware strom `v0.10e_nfc_show3` (EXP-054: tři grafiky). Host `ov26-nfc-show.sh` 1/2/3.

## JEDNA HLAVNÍ ZMĚNA

Latch `OVH` + n s `n <= 4`. Variant 4 volá `stream_zero_plane` místo RLE. Host menu 4.

## OČEKÁVANÝ VÝSLEDEK

UART boot `EXP-055 v0.10f NFC SHOW4` `ARMED`. Po `show 4` LED blikne, sklo ~15 s na bílou.

## SKUTEČNÝ VÝSLEDEK

Isolated flash DEV: CC2510, 28 KB write+verify OK. Recap UART:

```text
OpenVusion GU140 EXP-055 v0.10f NFC SHOW4
RESET_CAUSE=01
E2=00000000
UNLOCK already
ARMED
WAIT LED=00
```

SHOW4=1 v0.10f=1 ARMED=1. ROM 29494 / 32768. TAG ON, debugger isolated.

Lidský test volby 4 na skle: pending.

## KLASIFIKACE

UART boot **PASS**. Glass blank **pending** human `ov26-nfc-show.sh 4`.

## ZÁVĚR

Firmware přijímá latch 1–4. Volba 4 neukládá čtvrtou bitmapu — SPI nuly do 0x10/0x13.

## DALŠÍ KROK

`/home/hw/bin/ov26-nfc-show.sh` → **4**, držet TWN4 dokud LED zhasne; sklo by mělo zbělat ~15 s.
