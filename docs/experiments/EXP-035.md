# EXP-035 — RF-B profile IDLE

## Hypothesis

Writing `OVH_RF_PROFILE_0` then SCAL returns MARCSTATE IDLE and readback matches writable fields.

## Firmware

`v0.5b_rf_idle`. Flattened into `main.c` (linking `radio.rel` produced UART garbage). `--nooverlay`.

## UART (`captures/rf/ov26_exp035_por.bin`)

```text
CAL=01
MARC=01 SYNC=4F56 PKTCTRL0=45 FREQ=5D93C0 MDMCFG4=18 PA=44
MATCH=01 IDLE=01
HB 00..03 MARC=01
DONE
```

No TX. Isolated. TAG OFF.

## Result

**PASS.** Register readback matches profile. MCU stable.

## Next

RF-C bounded RX/RSSI.
