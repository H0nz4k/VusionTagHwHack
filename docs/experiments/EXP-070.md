# EXP-070 — User utilities tag-flash-latest / tag-send-image

## HYPOTÉZA

Pinned `v0.12b_nfc_epd` + existující `cli.py send` stačí na dvě krátké utility. Dry-run nesmí erase. Flash verify PASS. BIN 11248 projde DONE. BIN 11247 se odmítne před NFC.

## VSTUPNÍ STAV

Manifest `firmware/releases/latest.json`. SHA-256 `2ce75d6241c91d5bef98194515ba5024312fec006b156d485d35426015a927f6`. DEV tag.

## JEDNA HLAVNÍ ZMĚNA

`tools/tag-flash-latest` a `tools/tag-send-image`.

## SKUTEČNÝ VÝSLEDEK

Unit/mock: 11/11 PASS.

| Krok | Výsledek |
|---|---|
| `--dry-run` | OK, erase/write neproběhly |
| flash `--confirm-dev-tag --yes` | CC2510 unlocked, verify **PASS** |
| UART po flashi | `EXP-068 v0.12b OVMB-EPD`, `READY ER=00 GOT=0000` |
| BIN 11247 | rc=2, před TWN4, idle |
| `tag-send-image ovhack.bin` | DONE crc `D9331F86` 235 chunks, retries=0 |
| bench | `idle` |

Pre-erase backup HEX uložen, SHA `6b413811624bbcda3994e457ef6809b4f2b8b34ceae1f9108d686cef03775019`.

## KLASIFIKACE

**PASS**

## DALŠÍ KROK

Běžné použití viz `docs/FLASH_AND_IMAGE.md`.
