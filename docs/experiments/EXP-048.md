# EXP-048 — NFC show: baked graphic, FD trigger, LED only while drawing

## Hypothesis

A baked v0.4l-class image plus FD wait (LED off) and 250 ms blink only after FD=0 matches the reader-approach UX. Three hex files (32 kB cannot hold three planes).

## Graphics

1. `v0.9a_nfc_show1` — OpenVusionHack portrait (copy of v0.4l planes)
2. `v0.9b_nfc_show2` — landscape **TagHack** (Tag RED, Hack BLACK, centered) + **HanzG** black, smaller, bottom-right
3. `v0.9c_nfc_show3` — TagStudio `dithered_image.png` 296×152

Host:

```text
/home/hw/bin/ov26-nfc-show.sh
# or: ov26-nfc-show.sh 2
```

Mailbox still NACK — selection is which hex is flashed.

Landscape 2+3: first glass was scenery but mirrored. `ROTATE_90` was the wrong fix (looked like a 90 deg turn). Planes now `FLIP_TOP_BOTTOM` then `TRANSPOSE` (same scenery axis, vertical un-mirror).

## Result

Pending first human run of the menu script. Orientation: ROTATE_90 after mirrored TRANSPOSE.
