# EXP-040 — NFC-A I2C write-address 0xAA ACK

## Hypothesis

P0_4=SDA, P0_6=SCL bit-bang, 8-bit write address 0xAA, NTAG I²C Plus ACKs without driving P1_0.

## Firmware

`v0.6b_nfc_ack0`. ACK0 only. `--nooverlay`. No extra `.rel`. P0_2 untouched. No EEPROM write.

## UART (`captures/nfc/ov26_exp040_por.bin`)

Isolated flash **PASS**:

```text
OpenVusion GU140 EXP-040 v0.6b NFC-A ACK0
RESET_CAUSE=01
ACK0=01
HB 00
HB 01
HB 02
DONE
```

Repeat recapture after settle: same `ACK0=01`.

## Result

**PASS.** I2C slave at 0xAA **OVĚŘENO** on GU140. Pins P0_4/P0_6 **OVĚŘENO** for this ACK. P1_0 not required for ACK.

## Next

NFC-B session block 0xFE read (EXP-043).
